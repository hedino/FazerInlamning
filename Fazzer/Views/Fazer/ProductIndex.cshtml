@model Fazzer.ViewModels.ProductIndexViewModel
@{
    ViewBag.Title = "Index";
}
<h2>Products</h2>
<p>
    @Html.ActionLink("Create New", "ProductCreate")
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.ActionLink("Sort Name", "ProductIndex", new { Sort = Model.SortName })
            </th>
            <th>
                @Html.ActionLink("Sort Price", "ProductIndex", new { Sort = Model.SortPrice })
            </th>
        </tr>
    </thead>
    @foreach (var item in Model.Products)
    {
        <tr>
            <td>@item.Name</td>
            <td>@item.Price Kr</td>

            <td>
                @Html.ActionLink("Edit", "ProductEdit", new { id = item.ProductId }) |
                @Html.ActionLink("View", "ProductView", new { id = item.ProductId })
            </td>
        </tr>
    }
</table>

